# sdd-worker

`sdd-worker` dispatches task-scoped work to Codex, OpenCode, and future engines
using a Superpowers-centered software development workflow.

## Install

```sh
npm install -g sdd-worker
sdd-worker setup
```

Run `sdd-worker help` for the available commands. Use `sdd-worker doctor` to
check engine CLI setup after installation.
