name: codex
status: supported
default_runner: exec
capabilities:
  model_override: true
  read_only_sandbox: true
  workspace_write: true
  json_output: true
  file_attachments: false
  resume: true
defaults:
  sandbox:
    read_only: read-only
    workspace_write: workspace-write
  approval: never
command:
  run: codex exec

